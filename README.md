*This project has been created as part of the 42 curriculum by falves-e.*

# ft_printf

## Table of Contents

- [Description](#description)
- [Instructions](#instructions)
- [Function Prototype](#function-prototype)
- [Resources](#res)



## Description

`ft_printf` is a custom implementation of the standard C `printf()` function.

The goal of this project is to recreate the behavior of the original `printf` while learning about:

- Variadic functions in C
- Parsing formatted strings
- Handling different data types
- Memory and buffer management
- Low-level output using `write()`

This project is part of the 42 curriculum and focuses on improving understanding of formatted output functions and modular programming in C.

The function supports the following conversions:

| Conversion | Description |
|------------|-------------|
| `%c` | Prints a single character |
| `%s` | Prints a string |
| `%p` | Prints a pointer address |
| `%d` | Prints a decimal integer |
| `%i` | Prints an integer in base 10 |
| `%u` | Prints an unsigned decimal integer |
| `%x` | Prints a hexadecimal number (lowercase) |
| `%X` | Prints a hexadecimal number (uppercase) |
| `%%` | Prints a percent sign |

---

## Instructions

### Compilation

Clone the repository and compile the library using `make`:
 ```bash
git clone <your_repository_url>
cd ft_printf
make
```

This will generate the static library at the root of your directory:

```bash
libftprintf.a
```

### Available Makefile Rules

```bash 
make        # Compile the project
make clean  # Remove object files
make fclean # Remove object files and library
make re     # Recompile everything
```

### Usage

Include the header in your project:

``` C
#include "ft_printf.h"
```

Compile with the library:

```bash
cc main.c libftprintf.a
```

## Function Prototype

```C
int ft_printf(const char *format, ...);
```
The function returns the total number of printed characters, just like the original `printf()`.

## Resources

### Documentation & Tools

- Manual pages man

- The C Programming Language - Brian Kernighan & Dennis Ritchie

### AI
- AI tools were used throughout this project to improve documentation structure and also as a search engine.

- This project does not include any code generated by an AI.

- AI was used to help write this readme.
